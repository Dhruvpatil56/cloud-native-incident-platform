- rule: Unexpected Shell Inside Container
  desc: Detect shell execution inside container

  condition: container and spawned_process and shell_procs

  output: >
    Shell spawned inside container
    (user=%user.name container=%container.name shell=%proc.cmdline)

  priority: WARNING
